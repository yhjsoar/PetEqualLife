<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.petequallife">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PointActivity"></activity>
        <activity android:name=".ExpActivity" />
        <activity android:name=".PresentActivity" />
        <activity android:name=".NamingActivity" />
        <activity android:name=".CommentActivity" />
        <activity android:name=".AddCommunityActivity" />
        <activity android:name=".CommunityActivity" />
        <activity android:name=".FinishPlayActivity" />
        <activity android:name=".PlayActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".DogGroupInfoActivity" />
        <activity
            android:name=".SelectPopupActivity"
            android:theme="@android:style/Theme.Dialog" />
        <activity android:name=".SelectPetActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>